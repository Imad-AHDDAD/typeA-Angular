import { CustomUserDetails } from './custom-user-details';

describe('CustomUserDetails', () => {
  it('should create an instance', () => {
    expect(new CustomUserDetails()).toBeTruthy();
  });
});
